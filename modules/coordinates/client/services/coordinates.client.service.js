//Coordinates service used to communicate Coordinates REST endpoints
/*
(function () {
  'use strict';

  angular
    .module('coordinates')
    .factory('CoordinatesService', CoordinatesService);

  CoordinatesService.$inject = ['$resource'];

  function CoordinatesService($resource) {
    return $resource('api/coordinates/:coordinateId', {
      coordinateId: '@_id'
    }, {
      update: {
        method: 'PUT'
      }
    });
  }
})();
*/
